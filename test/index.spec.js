const { test } = require("node:test");
const { fastifyClientGenerator } = require("../index.js");
const fastify = require("fastify");

test("Correctly generates OpenAPI specification", async () => {});
